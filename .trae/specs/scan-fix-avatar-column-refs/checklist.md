- [x] Mapper 扫描结果覆盖全部 XML 并输出问题文件清单
- [x] 帖子、回复、聊天、用户相关 SQL 已完成头像列兼容修复
- [x] 查询结果统一输出可映射到 `avatarPath` 的字段别名
- [x] 主列路径与兼容路径均通过回归验证
- [x] 自动扫描复查后不再存在遗漏的头像列不一致引用

- 环境限制记录：当前机器未安装 Maven（`mvn` 不可识别），本次以静态扫描与 IDE 诊断替代命令回归。
