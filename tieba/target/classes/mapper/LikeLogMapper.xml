<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chatroom.tieba.mapper.LikeLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO forum_like_log(user_id, target_id, target_type, created_at)
        VALUES(#{userId}, #{targetId}, #{targetType}, NOW())
    </insert>

    <delete id="deleteByUserAndTarget">
        DELETE FROM forum_like_log
        WHERE user_id = #{userId} AND target_id = #{targetId} AND target_type = #{targetType}
    </delete>

    <select id="findByUserAndTarget" resultType="ForumLikeLog">
        SELECT * FROM forum_like_log
        WHERE user_id = #{userId} AND target_id = #{targetId} AND target_type = #{targetType}
        LIMIT 1
    </select>

</mapper>
