<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 组件扫描: 只扫描 @Controller -->
    <context:component-scan base-package="com.chatroom.tieba.controller" />
    <context:property-placeholder location="classpath:application.properties" ignore-resource-not-found="true" />

    <!-- 启用 Spring MVC 的注解驱动 -->
    <mvc:annotation-driven />

    <!-- 静态资源放行 -->
    <mvc:resources mapping="/static/**" location="/static/" />

    <!-- Servlet 3.0 文件上传解析器 -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.support.StandardServletMultipartResolver" />

    <!-- JSP 视图解析器 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/jsp/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!-- 拦截器配置 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <!-- 需拦截的路径（如发帖、登出、用户中心、聊天室等） -->
            <mvc:mapping path="/post/**"/>
            <mvc:mapping path="/thread/create/**"/>
            <mvc:mapping path="/chat/**"/>
            <mvc:mapping path="/user/**"/>
            <mvc:mapping path="/api/user/**"/>
            <mvc:mapping path="/auth/logout"/>
            
            <!-- 放行静态资源和登录注册等 -->
            <mvc:exclude-mapping path="/static/**"/>
            <mvc:exclude-mapping path="/auth/login"/>
            <mvc:exclude-mapping path="/auth/register"/>
            
            <bean class="com.chatroom.tieba.interceptor.LoginInterceptor" />
        </mvc:interceptor>
    </mvc:interceptors>

</beans>
